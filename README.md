# Telegram Review Bot

Телеграм-бот для сбора и модерации отзывов с двумя типами пользователей:

- **Обычные пользователи** — оставляют оценку, текст и фото, листают отзывы по 5 штук на страницу.
- **Администраторы** — настраивают приветственный пост (текст + фото/видео) и отвечают на отзывы прямо из Telegram.

## Основные возможности

- приветственный пост с медиа, редактируемый из админ-панели;
- пошаговый сбор отзывов: рейтинг 1–5, текст, опциональное фото;
- просмотр отзывов с пагинацией (5 на страницу) и просмотр фото по кнопке;
- админ-режим показывает автора отзыва и даёт возможность ответить пользователю;
- SQLite-хранилище с автоинициализацией.

## Стек

- Python 3.12+
- [Aiogram 3](https://docs.aiogram.dev/)
- SQLite

## Структура проекта

```
.
├── admin/           # Админские хендлеры
├── db_manager/      # Работа с SQLite
├── logic/           # FSM и бизнес-логика отзывов
├── menu/            # Команда /start и клавиатуры
├── utils/           # Логирование и проверки прав
├── main.py          # Точка входа
└── README.md
```

## Переменные окружения

Создайте `.env` в корне проекта:

```
BOT_TOKEN=123456:ABCDEF               # токен @BotFather
OWNER_ID=123456789                    # ваш Telegram ID
ADMIN_IDS=123456789,987654321         # через запятую, без пробелов
```

`OWNER_ID` автоматически получает админские права, остальные админы берутся только из `ADMIN_IDS`.

## Локальный запуск

```bash
python -m venv .venv
. .venv/Scripts/activate      # Windows PowerShell
pip install -r requirements.txt
python main.py
```

Бот использует SQLite в `database/database.db`. Файл и папка создаются автоматически.

## Деплой на Railway

1. Залогиньтесь на [railway.app](https://railway.app/) и создайте новый проект.
2. Подключите репозиторий GitHub или загрузите архив вручную.
3. В разделе **Variables** добавьте переменные окружения из `.env`.
4. В **Deployments → Settings** укажите стартовую команду `python main.py`.
5. Нажмите **Deploy**. Railway автоматически подтянет зависимости из `requirements.txt` и запустит бота.

> Совет: включите авто‑redeploy по пушу в `main` или используйте Railway CLI (`railway up`) для локальной отладки.

## Как пользоваться ботом

### Обычный пользователь

1. Жмёт `/start` и видит приветственный пост с кнопками:
   - `📝 Оставить отзыв` — проходит через FSM (рейтинг → текст → фото).
   - `📖 Посмотреть отзывы` — листает по 5 отзывов, может открыть фото.
2. После отправки отзыва получает подтверждение.

### Администратор

1. `/start` показывает три кнопки:
   - `✏️ Изменить привет` — загружает новый текст и необязательное фото/видео.
   - `📚 Посмотреть отзывы` — та же пагинация, но с данными автора, кнопками «Ответить №X» и «🗑️ Удалить №X».
   - `✅ Модерация отзывов` — просмотр неодобренных отзывов с возможностью одобрить, отклонить или удалить.
2. При ответе пользователю сообщение отправляется сразу в личку, а текст ответа сохраняется в карточке отзыва.
3. Все новые отзывы требуют модерации — они не видны пользователям до одобрения администратором.

## Полезные команды

```bash
python main.py                # запустить локально
pip install -r requirements.txt
python -m compileall .        # быстрая проверка синтаксиса
```

## Лицензия

Проект распространяется без лицензии. Используйте и дорабатывайте под свои задачи. Contributions welcome!

